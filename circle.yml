machine:
  node:
    version: 4.3.0

test:
  override:
    - echo "No tests! This is APIFORT"

deployment:
  production:
    branch: master
    commands:
      - AWS_STAGE=prod ./deploy.sh
  staging:
    branch: [develop, lineometer]
    commands:
      - AWS_STAGE=dev ./deploy.sh
