deployment:
  production:
    branch: develop
    commands:
      - AWS_STAGE=dev ./deploy.sh
  staging:
    branch: develop
    commands:
      - AWS_STAGE=dev ./deploy.sh

test:
  override:
    - echo "No tests! This is a Wordpress theme"
